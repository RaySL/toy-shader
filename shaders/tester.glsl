#version 150
NOPE; int 6 ; $$%% #define #$$%# 45 ;;
while )( }{
eat soup 6
4re in ded
uniform e = 0;
uniform float time;

out vec4 fragColor;

void main(void)
{
  fragColor = vec4(gl_FragCoord.xy, sin(time*4.0)*0.5+0.5, 1.0);
}
